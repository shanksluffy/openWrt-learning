# 统计式 AIOps 落地方案：基线异常检测 · 横向对比 · 根因规则引擎

> 无线网络运维智能化的第一步实现方案。全程不含模型训练、不做在线闭环控制，只用稳健统计量加规则引擎，拿到 AIOps 的大部分价值。
>
> 配套文档：
> [`ac-ap-networking-guide.md`](./ac-ap-networking-guide.md)（组网方案与转发模式）·
> [`ac-ap-software-architecture.md`](./ac-ap-software-architecture.md)（软件系统架构，第八章为 AI 路径总览）
>
> 本文是该架构文档 8.2 节"第一梯队：运维智能化"的展开，只讲执行步骤和原理。

## 目录

- [一、总体设计](#一总体设计)
- [二、数据底座](#二数据底座)
- [三、引擎一：基于基线的异常检测](#三引擎一基于基线的异常检测)
- [四、引擎二：同型号 AP 横向对比](#四引擎二同型号-ap-横向对比)
- [五、引擎三：故障根因规则引擎](#五引擎三故障根因规则引擎)
- [六、实施排期与上线策略](#六实施排期与上线策略)
- [附录 A：常见翻车点速查](#附录-a常见翻车点速查)
- [附录 B：自我度量指标](#附录-b自我度量指标)

---

## 一、总体设计

三个引擎共用一套数据底座，各自负责一个维度：

| 引擎 | 维度 | 回答的问题 | 擅长发现 |
|---|---|---|---|
| 基线异常检测 | 时间（纵向） | 这台 AP 跟它自己平时不一样 | 突变、渐变 |
| 同型号横向对比 | 空间（群体） | 这台 AP 跟同伴不一样 | 先天不良、个体劣化 |
| 根因规则引擎 | 因果 | 不一样的原因在哪个责任域 | 故障归位 |

前两个是**检测器**，第三个是**分类器**。只有检测没有根因等于制造告警噪音；只有根因没有检测则没有触发源。三者必须都做。

### 建设顺序不等于编号顺序

- **横向对比排在纵向基线之前**：它不需要历史数据积累，数据一通就能出结果，还能给基线的冷启动期兜底。
- **规则引擎必须最后做**：它消费前两者的输出。
- 两个检测引擎**互为冷启动兜底**：基线未成熟时靠横向对比，横向分组不足时靠纵向基线。

### 有意不做的事

不做模型训练，不用 GPU，不自动改信道功率，不做任何在线闭环控制。所有输出都是"结论 + 证据链 + 建议动作"，交人工确认。AP 侧只增加采集与聚合，云侧是 SQL 加上百来行统计代码和一个规则解释器。

**这就是"低风险"的具体含义。** 同时它也不是机器学习的死胡同——见 [5.4 节](#54-规则的工程化要求)，它顺手把后续做模型唯一稀缺的东西（标注数据）攒了出来。

---

## 二、数据底座

### 2.1 指标选型的唯一标准：可比性

这是整个方案最容易做错的一步。指标必须是**归一化的比率或效率**，不能是绝对量。原因很直接：横向对比要求"同伴的表现就是这台 AP 本应有的表现"，而绝对吞吐、终端数、绝对空口占用率都取决于 AP 装在哪、人在哪，天生不可比。

| 可比（横向对比只能用这些） | 不可比 |
|---|---|
| 重传率、tx_failed 率 | 绝对吞吐 |
| 关联成功率、认证耗时 p95 | 在线终端数 |
| 干扰占比、噪声底 | 绝对 channel busy |
| 速率达成率 | 绝对 RSSI 分布 |
| 有线口错误率 | 每 AP 流量总量 |
| 每终端平均 airtime 效率 | — |

其中有一个派生指标价值远高于其他，值得单独列出：

```
干扰占比 = (busy_time - tx_time - rx_time) / active_time
```

四个量都来自 nl80211 survey dump 的累计计数器。它的物理意义是"被别人占用的空口时间"，能干净地区分**自己忙**和**被干扰**。绝大多数"上网慢"的鉴别诊断都要靠它。

### 2.2 采集清单

| 层级 | 周期 | 指标 | 采集点 |
|---|---|---|---|
| 射频 | 60s | busy/tx/rx/active time、噪声底、同频邻居数与最强邻居 RSSI | nl80211 survey dump、scan、`usteer` |
| 连接过程 | 事件 + 5min 聚合 | assoc/auth 成功率与耗时分布、EAPOL 超时、RADIUS RTT 与超时数、deauth 按 reason code 分桶 | `hostapd` ubus 事件与 RADIUS 统计 |
| 终端质量 | 采样后聚合成分位数 | RSSI、SNR、重传率、tx_failed 率、协商 MCS/NSS/带宽、per-STA airtime | station dump、`get_clients` |
| 系统健康 | 60s | MemAvailable 趋势、CPU、有线口协商速率/双工/CRC 错误、温度、uptime 与重启原因 | `/proc`、`/sys/class/net`、ethtool 计数 |

> 各采集点在软件栈中的位置见 [`ac-ap-software-architecture.md`](./ac-ap-software-architecture.md) 8.1 节。

### 2.3 采集与聚合的四条硬规则

**1. Counter 上报原始累计值，不在 AP 上算速率。**
连同一个单调时钟戳一起上报，云端做差分。必须显式检测 counter 回绕或重置（AP 重启），否则会产生负速率和巨大尖刺——这是最常见的脏数据来源。

**2. 60 秒窗口内聚合成 `(count, sum, sumsq, min, max, 直方图)`。**
存 `sum` 和 `sumsq` 是为了云端能跨窗口重算均值方差（这两个量可结合）。分位数**必须用直方图或 t-digest 合并**，绝不能把各 AP 的 p95 求平均——**分位数不可平均**，这个错误在这类系统里极其普遍且难以察觉。

**3. 事件流全量上报，不聚合。**
量小、信息密度极高，尤其 802.11 reason code 是根因分析唯一可靠的信号源。只对事件风暴做限速，且限速时要补记被丢弃的计数。

**4. 自有动作必须打标。**
`usteer` 主动引导、RSSI 门限踢除、定时重启，全部要带自有标记，与终端发起的断开严格区分。否则**自己的漫游策略会被诊断成故障**，这是极常见的自打脸。

数据量粗算：每 AP 每分钟约 50 个指标 × 7 个统计量 ≈ 压缩后 1~2KB。1000 台 AP 约 3GB/月，完全可承受。

### 2.4 存储选型

时序数据建议直接用 ClickHouse 而非 Prometheus 类 TSDB。理由是横向对比和根因查询的形态都是"取某一时刻的切片，在跨设备维度上做分位数聚合"，这是 OLAP 擅长而 TSDB 不擅长的。事件流放同库另一张表。

**维表是被忽略最多、但决定成败的部分：**

- AP 型号与硬件版本
- 固件版本
- AP Group、楼层与场景类型（办公/宿舍/会议室/走廊）
- 上行交换机及端口
- 射频配置快照（信道、带宽、功率、国家码）
- SSID 配置指纹

配置快照必须**版本化**（每次变更留一条记录），否则"变更引起的故障"这一类——占突发故障一半以上——永远查不出来。上行交换机端口尽量用 LLDP 自动发现，减少人工维护。

### 2.5 时间对齐

NTP 是硬前提（见组网指南第二章第 7 节）。此外还要做两件事：

1. AP 上报时带自己的采集时间戳，与云端接收时间偏差超过 2 秒的 AP 单独告警，**并从群体统计中剔除**——脏数据比没数据危害大。
2. 聚合窗口按整分钟对齐（floor to minute），否则横向对比的"同一时刻"根本不成立。

---

## 三、引擎一：基于基线的异常检测

### 3.1 原理

核心假设是无线指标具有**强周期性**，因为它由人的作息驱动。所以异常的定义不是"超过固定阈值"，而是"偏离它自己在一周中同一时间片段的历史分布"。

固定阈值的失败模式很具体：会场 AP 周一上午 channel busy 85% 完全正常，而深夜 40% 反而是重大异常（说明存在非业务干扰源）。**单一阈值无论定成多少都是错的。**

### 3.2 时间分桶

把一周切成 **7 × 24 × 4 = 672 个 15 分钟桶**，每个实体的每个指标维护 672 条基线。

15 分钟是经验最优粒度：更细则每桶样本不足、噪声主导；更粗则抓不住高峰启动和结束的转折。

每桶每周只有一个样本，四周才四个，样本仍偏少。缓解办法是**邻桶借样本**：桶 `t` 的基线用 `t-1, t, t+1` 三桶合并，等价于滑动窗口平滑，把有效样本量提到 12 个左右。

冷启动期（不足两周数据）退化为横向对比加宽松固定阈值。

### 3.3 用稳健统计量，不要用均值加标准差

`μ ± 3σ` 在这里必然失效，有两个独立原因：

1. 无线指标分布严重右偏且长尾，正态假设不成立。
2. 更致命的是**异常样本本身会污染 μ 和 σ**。均值和标准差的崩溃点为 0，一个极端值就能把基线拉走。

正确做法是双轨：

**判定用经验分位数。** 每桶记录 p05/p50/p95/p99，越界即"超出 p99"或"低于 p05"。分位数法对分布不做任何假设，且最容易向运维解释。

**严重度排序用稳健 z 分数：**

```
z = (x - median) / (1.4826 * MAD + ε)
```

`MAD` 是中位绝对偏差，`1.4826` 是使其在正态分布下与 σ 一致的换算常数，`ε` 用于防止常量指标 MAD 为 0 时结果爆炸。

### 3.4 基线更新与污染时段排除

用近 4 周滑动窗口，每天凌晨批量重算一次。1000 AP × 50 指标 × 672 桶的规模完全算得动，不需要增量算法的复杂度。滑动窗口天然带遗忘，能自适应开学、放假这类季节变化。

**重算时必须剔除已确认的异常时段和变更时段。** 为此要维护一张"污染时间段"表，内容包括：

- 已确认告警的时间区间
- 配置变更时刻
- 固件升级窗口

不做这件事，一次持续三天的故障会被学成正常状态，之后再也告不出来。

### 3.5 降噪：这一步决定系统生死

**单点越界绝不告警。** 三层门控叠加：

| 门控 | 规则 | 目的 |
|---|---|---|
| 持续性 | 滑动窗口 5 取 3 越界才触发 | 用滑动计数而非要求连续，容忍单点回落 |
| 幅度 | 每个指标定义"最小有感差异"（如 RSSI < 3dB、成功率 < 2% 不算） | 统计显著 ≠ 用户有感 |
| 样本量 | 在线终端数 < 3 时终端质量类指标不参与判定；深夜无人时段屏蔽 | 分位数在小样本下无意义 |

幅度门控这一条比统计显著性更重要——运维只关心用户有感的问题。

在此之上做**告警聚合**：

- 同一实体多指标同时异常 → 合并成一个事件，交给规则引擎定根因
- 同一楼层或同一上行交换机下多台 AP 同时异常 → 升级为区域事件，只告一条并标注疑似共因
- 抑制窗口：同一 `(实体, 指标)` 在 M 分钟内不重复告警，只更新事件状态

这一步是把 100 条噪音变成 1 条有用告警的关键。

### 3.6 慢变量做趋势外推，这就是"故障预测"

对内存可用量、上行口 CRC 累计错误、DHCP 池占用率、RADIUS RTT 这类单调或缓变指标：

1. 用 **Theil-Sen 稳健回归**拟合近 24~72 小时的斜率（不用最小二乘，因为要抗离群点）
2. 外推出"按当前趋势多久触顶"
3. 小于 7 天则发预测性告警

这就是"内存泄漏导致 AP 即将掉线""DHCP 池即将耗尽"的完整实现方式，**一行机器学习代码都不需要**。

---

## 四、引擎二：同型号 AP 横向对比

### 4.1 原理

纵向基线只能发现"变了"，发现不了"一直就烂"。而现场大量问题是**从交付第一天就存在的**：

- AP 装在金属吊顶内、承重柱背面
- 天线没接好
- 网线只通四芯
- 配置漂移
- 个别 AP 硬件性能不良

这类问题会被纵向基线学成"正常"，永远告不出来。

横向对比的统计基础是：在合适的分组下，同伴群体构成一个天然对照组，**组内中位数就是"这台 AP 本应有的表现"**。本质上是用空间维度替代那个不可能做的对照实验。

### 4.2 分组（Cohort）定义决定成败

分组维度不足，就全是假阳性。分组键至少要包含：

| 维度 | 理由 |
|---|---|
| AP 型号 + 硬件版本 | 射频性能天然不同 |
| 固件版本 | 驱动差异能造成 20% 指标差 |
| 射频配置（频段/带宽/功率档/国家码） | 直接决定指标量级 |
| 场景标签（楼层类型） | 走廊与房间的环境完全不同 |
| **同一时刻在线终端数分桶**（0 / 1-5 / 6-15 / 16-30 / 30+） | 控制负载这个最大的混淆变量 |

最后一项常被漏掉，但它等价于做条件对比，缺了它假阳性会失控。

组内至少 8 台才做对比，少于 8 台中位数不稳定。不足时逐级放宽维度（先合并场景标签，再合并固件版本），同时降低告警置信度。

> **最大软肋**：场景标签靠人工维护。所以要把楼层、场景类型、上行交换机端口做成交付流程的**强制字段**，端口尽量用 LLDP 自动发现。标签质量直接等于这个引擎的可用性。

### 4.3 判定

在同一时间窗口、同一 cohort 内：

1. 算组内 median 与 MAD 得稳健 z 分数，或直接算**组内百分位排名**（更好解释："这台 AP 的重传率排在同组第 98 百分位"）
2. 判定条件是**持续离群**：24 小时内有效窗口的 80% 以上都落在末尾 5%
3. 严重度 = 离群程度 × 受影响终端数

一次性排名靠后毫无意义，必须要求持续性。

### 4.4 几条零成本、高命中率的具体检测

这几条几乎不需要调优就能出结果，建议作为首批交付内容。

**速率达成率离群 → 天线未接好、射频链路损坏或金属遮挡。**
原理是把 `(RSSI, 协商 MCS)` 做联合统计，**在相同 RSSI 分桶内**比较 MCS。若 MCS 显著低于同伴，说明问题在硬件或安装，而不在环境——因为环境差会同时压低 RSSI，而硬件问题只压低给定 RSSI 下的效率。这是整套方案里最漂亮的一条推断。

**有线口协商速率或双工离群 → 网线故障、水晶头氧化、交换机口老化。**
同型号 AP 大家都 1000M 全双工，一台 100M 半双工可以直接断言。零假阳性。

**CRC 错误率离群 → 线路质量。** 同样零假阳性。

**噪声底比同楼层高 6dB 以上 → 附近有干扰源。**

**重启次数离群 → PoE 供电不足或硬件不良。**
再结合"掉线时刻是否与该交换机 PoE 功率峰值相关"可进一步区分供电不足与软件崩溃。

**配置漂移检测。**
把 UCI 配置做归一化后哈希，同一 AP Group 内哈希不一致的直接列出差异字段。严格说这不是统计方法，但它在同一框架下最容易加，而且是客户最爱看的功能之一。

### 4.5 两个检测引擎的分工

| 命中情况 | 含义 | 处理方式 |
|---|---|---|
| 纵向 + 横向同时命中 | 高置信度真故障 | 最高优先级实时告警 |
| 仅纵向命中 | 新发生的变化 | 实时告警 |
| 仅横向命中，且长期稳定不变 | 交付质量问题 | **周期巡检报告，不做实时告警** |

最后一行的区分很重要：长期稳定的离群不是"新事件"，做成实时告警只会制造疲劳。把它们做成交付质量验收报告，既避免噪音，又额外产出一个可独立售卖的能力。

---

## 五、引擎三：故障根因规则引擎

### 5.1 定位：不要定位"根因"，要定位"责任域"

前两个引擎输出症状，这个引擎做症状到故障域的映射。它不做因果推断，本质是**把资深工程师的排查树代码化**。

关键的设计取向是：运维实际需要的判断是"这单派给谁"——无线组、网络组、认证/DHCP 组，还是终端侧问题不予受理。

所谓"60% 以上的 Wi-Fi 投诉根本不在 Wi-Fi 上"，能自动证明这一点就是最大价值，因为它省掉的是最贵的两样东西：**无谓的现场上门，和跨部门扯皮**。

### 5.2 三段式结构：证据 → 规则 → 结论

#### 证据层（Predicate）

把指标和事件抽象成独立可单测的布尔或分级谓词：

```
assoc_fail_high        eapol_timeout_high     radius_rtt_high
dhcp_pool_near_full    dhcp_nak_present       interference_high
uplink_crc_rising      uplink_speed_degraded  uplink_saturated
sta_weak_rssi_ratio    sticky_client_present  retry_rate_outlier
noise_floor_outlier    cochannel_neighbors_high
config_changed_recently   firmware_changed_recently
```

reason code 谓词要按**语义**分类而非按码值罗列：

| reason code | 语义 | 指向 |
|---|---|---|
| 15（四次握手超时）、2 | 密钥协商失败 | 认证/密钥 |
| 5（AP 无资源） | 容量耗尽 | 容量 |
| 3 / 8（STA 主动离开） | 多为正常漫游 | 通常非故障 |
| 34 | 低 ACK | 链路质量 |
| 1 / 4（未指定/不活跃） | 终端休眠或空口断 | 需其他证据鉴别 |

并且**必须把 AP 主动发出的 deauth 与终端发起的分开统计**，前者是自有策略行为（见 [2.3 节第 4 条](#23-采集与聚合的四条硬规则)）。

#### 规则层（Rule）

形式：

```
IF 证据组合 AND 排除条件 THEN 结论(故障域, 原因, 置信度, 建议动作, 证据链)
```

带排除条件的**鉴别诊断**是它与朴素 if-else 的本质区别：

| 症状 | 关键鉴别证据 | 结论与责任域 |
|---|---|---|
| 关联失败 | EAPOL 超时高、RADIUS 正常、仅个别终端 | 终端密钥/证书，不予受理 |
| 关联失败 | EAPOL 超时高、RADIUS 超时高、多 AP 同现 | RADIUS 服务器，认证组 |
| 关联失败 | assoc 阶段即失败、reason 5、终端数触顶 | 容量不足，无线组扩容 |
| 上网慢 | 重传率高、干扰占比高、同频邻居多 | 同频干扰，无线组调信道 |
| 上网慢 | 重传率正常、空口空闲、上行口饱和 | 回传带宽，网络组 |
| 上网慢 | 空口与回传均正常、DNS p95 高 | DNS 服务，非 Wi-Fi |
| 部分站点打不开 | 仅隧道转发 SSID、大包失败小包正常 | MTU/分片，钳 MSS |
| 拿不到 IP | 有 discover 无 offer、多 AP 同现 | DHCP 服务或中继，网络组 |
| 拿不到 IP | 有 offer、request 后 NAK、池占满 | 地址池耗尽 |
| 拿不到 IP | 仅限某几台 AP，且同属一台接入交换机 | 业务 VLAN 未放通 |
| AP 掉线 | uptime 归零、掉线时刻与该交换机 PoE 峰值相关 | PoE 预算不足 |
| AP 掉线 | uptime 归零、掉线前内存可用量持续下降 | 固件内存泄漏 |
| AP 掉线 | uptime 未归零（仅管理面断）、CRC 上升 | 线路质量 |
| AP 掉线 | 多 AP 同时、AC 侧 CPU 高 | 注册风暴 |
| 漫游卡顿 | 11r 未协商成功、集中在特定终端 OUI | 终端兼容性，该类走 FT-over-DS 或独立 SSID |
| 漫游卡顿 | 邻居 AP 存在但终端不切、低速率未关 | 配置问题，关低速率并调门限 |
| 覆盖差 | p50 RSSI 与速率达成率同时横向离群 | 安装或天线，需现场 |
| 覆盖差 | p50 RSSI 正常、弱信号终端占比集中在特定时段 | 人员分布变化，需加 AP |

> 每一条对应的工程背景见组网指南[第二章](./ac-ap-networking-guide.md#二典型工程问题与优化方案)与[第三章](./ac-ap-networking-guide.md#三容易被忽略的细节)。

#### 结论层（Verdict）

每条结论必须输出四样东西，缺一不可：

1. 故障域 + 一句话原因
2. **证据链**：触发了哪些谓词、当时数值与基线值各是多少
3. 建议动作：具体到"改哪个参数、查哪个端口"
4. 置信度：规则先验 × 证据强度

证据链是硬要求。**没有证据链的结论运维不会信，而运维只要不信一次就永久不再看这个系统。**

### 5.3 冲突消解顺序

多条规则同时命中时的裁决顺序，本质上是复刻工程师的排查顺序：

**1. 变更优先。**
最近有配置或固件变更的，优先归因于变更。经验上超过一半的突发故障来自变更，这条规则的性价比高于其余所有规则之和。

**2. 共因收敛。**
影响多台 AP 的原因压制影响单台的原因。具体做法是用拓扑做集合比对：若异常 AP 集合与"某台接入交换机下的 AP 集合"重合度高（算 Jaccard 相似度），判为该交换机或其上行链路的共因，抑制所有下游 AP 的个体告警。

> 这是根因引擎里**最有价值的单个功能**。它决定了一次核心交换机抖动是产生 1 条告警还是 500 条。

**3. 层级优先，越底层越优先：**

```
供电 → 链路 → 二层/VLAN → 三层/DHCP → 认证 → 空口 → 终端
```

空口问题永远放在最后判定，因为它最容易成为其他一切问题的表象。

**4. 同层级内**按 `置信度 × 影响终端数` 排序。

### 5.4 规则的工程化要求

**声明式表达。** 规则用 YAML/JSON 描述并支持热加载，不要硬编码。理由不只是灵活性：现场工程师能自己加规则，这件事本身就是产品护城河。阈值抽成变量，支持按 AP Group 覆盖。

**可度量、可淘汰。** 每条规则要有唯一 ID、版本、命中计数和准确率统计。界面上给运维一个"这个诊断对不对"的按钮，用反馈自动关闭低准确率规则。

> 顺带说一句：这批反馈数据就是将来做真正机器学习时唯一稀缺的东西——**标注样本**。所以统计式方案不是机器学习的替代品或死胡同，它是必经的前置阶段，数据飞轮在这里开始转。

**可回放。** 拿历史一周数据跑一遍新规则，看命中量和准确率再决定是否上线。没有回放能力，规则调优就是盲改。

---

## 六、实施排期与上线策略

以 3~5 人小组估算：

| 阶段 | 内容 | 周期 | 验收标准 |
|---|---|---|---|
| P0 | 数据底座：采集 agent、传输、schema、维表、时间校验 | 4~6 周 | 1000 AP 下开销 < 1% CPU、< 2KB/min/AP，完整率 > 99% |
| P1 | 可视化与体验评分 | 2~3 周 | 先出人能看的东西，换现场信任与反馈 |
| P2 | 横向对比 | 2~3 周 | 首个交付物做成巡检报告而非实时告警 |
| P3 | 纵向基线 | 3~4 周开发 + 3~4 周攒数据 | 与 P2 并行开发，数据够了再开告警 |
| P4 | 规则引擎 | 4~6 周起，持续迭代 | 首批 10~15 条高频规则覆盖 80% 工单 |
| P5 | 反馈闭环与回放平台 | 持续 | 准确率可度量、规则可淘汰 |

P1 的体验评分口径：连接成功率、连接耗时、空口质量、吞吐达成率、漫游成功率加权。它不产生新信息，但它是所有价值的载体——**没有量化，优化效果就无法证明**。

P4 首批规则建议覆盖：AP 上线失败、AP 掉线、DHCP 失败、认证失败、同频干扰、MTU 分片。这六类占现场工单的绝大多数。

### 必须走影子模式

上线前 4~8 周**只记录不推送**，人工比对系统诊断与实际工单结论，统计准确率。达到下面两个条件才开始推送：

- 真阳性率 > 80%
- 每 AP 每周告警数 < 0.5 条

这个门槛看起来保守，但**告警疲劳是这类系统唯一的死因，而且是不可逆的**——运维一旦开始忽略告警，就再也不会回头看。宁可漏报，不可滥报。

---

## 附录 A：常见翻车点速查

| # | 翻车点 | 后果 | 对策 |
|---|---|---|---|
| 1 | 分组标签人工维护、标错 | 横向对比全是假阳性 | 关键标签设为交付强制字段，端口用 LLDP 自动发现 |
| 2 | 基线被故障期数据污染 | 故障被学成正常，此后永不告警 | 维护污染时段表，重算时剔除 |
| 3 | counter 重置未处理 | 负速率、异常尖刺 | 显式检测回绕与重置 |
| 4 | 分位数被求平均 | 统计上直接错误且难察觉 | 用直方图或 t-digest 合并 |
| 5 | 自有策略动作未打标 | 自己的踢除和定时重启被诊断成故障 | 所有 AP 主动动作带自有标记 |
| 6 | 规则库无治理地膨胀 | 准确率下降、无法维护 | 命中统计 + 准确率淘汰机制 |
| 7 | 只做检测不做共因收敛 | 一次上行抖动淹没告警面板 | 拓扑集合比对，收敛为区域事件 |
| 8 | 深夜/无人时段数据稀疏 | 大量无意义告警 | 样本量门控 |

---

## 附录 B：自我度量指标

必须内置，它们既是技术指标也是销售材料：

| 指标 | 含义 |
|---|---|
| 告警精确率 | 人工确认真阳性 / 总告警数 |
| 告警召回率 | 工单中被提前告警的比例 |
| MTTR 变化 | 接入前后对比 |
| 工单自动归类准确率 | 责任域判定正确率 |
| **判定为"非 Wi-Fi 问题"的占比** | 客户第一次看到这个数字时反应通常最强烈 |
| 免上门率 | 直接换算人力成本节省 |
