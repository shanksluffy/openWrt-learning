# AC/AP 工程组网方案指南

> 内容涵盖：常见组网形态、典型工程问题与优化方案、直接转发与隧道转发的原理对比与选型，以及在 OpenWrt 上的对应实现点。
>
> 配套文档：[`ac-ap-software-architecture.md`](./ac-ap-software-architecture.md)（软件系统架构：OpenWrt · 驱动 · Linux 内核）

## 目录

- [一、常见组网形态](#一常见组网形态)
- [二、典型工程问题与优化方案](#二典型工程问题与优化方案)
- [三、容易被忽略的细节](#三容易被忽略的细节)
- [四、直接转发 vs 隧道转发](#四直接转发-vs-隧道转发)
- [五、转发模式选型](#五转发模式选型)
- [六、在 OpenWrt 上的落地](#六在-openwrt-上的落地)
- [附录：交付验收指标](#附录交付验收指标)

---

## 一、常见组网形态

### 1. 胖 AP（FAT AP）独立组网

每台 AP 自带完整配置和转发能力，靠 Web/SSH 单独管理。适合 3~5 台以内的小场景。超过十几台后运维成本会失控：配置漂移、批量升级困难、漫游体验差。

### 2. 瘦 AP + AC（最主流）

AP 只做射频和 CAPWAP 隧道端点，配置、认证、漫游、射频调优由 AC 集中完成。有两个**正交**的维度：

**部署位置**

- **旁挂**：AC 挂在核心交换机旁，业务流不强制过 AC。扩容方便，是大部分校园/园区的选择。
- **直连/串接**：AC 位于转发路径上。管理简单，但 AC 成为带宽和故障的瓶颈。

**转发方式**

- **集中转发（隧道转发）**：用户报文封装进 CAPWAP 数据隧道回到 AC 再解封装。全网大二层、漫游 IP 不变、策略集中；代价是 AC 吞吐瓶颈、时延增加、MTU 变小。
- **本地转发（直接转发）**：AP 直接把报文按 VLAN 送到接入交换机。性能好、AC 只管管理面；但跨 VLAN 漫游会断，策略要下沉。

> 详见 [第四章](#四直接转发-vs-隧道转发)。

### 3. 云 AC / 控制器上云

AC 功能放到公有云或本地服务器（OpenWrt 生态里如 OpenWISP、TR-069/ACS），AP 走 NAT 穿透注册。适合多分支、连锁门店。主要风险在断网逃生和公网链路稳定性。

### 4. Mesh / 无线回传

布线不可达时使用。每跳吞吐约打对折（除非有专用回传射频），一般不建议超过 2 跳。

### 5. 网关型一体机（路由器兼 AC）

中小企业、酒店常见。AC + 网关 + 认证一体，成本低但单点风险集中。

---

## 二、典型工程问题与优化方案

### 1. AP 上线与发现

**现场最高频故障。** AP 找不到 AC 的常见原因：

- AP 与 AC 不在同一二层域，而三层发现没配 DHCP Option 43（华为等厂商用 Option 138）
- 中间防火墙/ACL 挡了 CAPWAP 的 **UDP 5246（控制）** 和 **UDP 5247（数据）**
- AP 管理 VLAN 的 DHCP 池耗尽
- PoE 供电不足导致 AP 反复重启，表象酷似"注册失败"

**排查顺序**：确认 AP 拿到管理 IP（接入交换机看 MAC 表和 DHCP 绑定）→ 确认能 ping 通 AC → 抓 5246 包看有无 Discovery Request/Response。

**规范做法**：管理 VLAN 单独规划；DHCP 静态下发 Option 43；AC 上开 AP 白名单（按 SN 或 MAC）防止私接 AP 注册。

**注册风暴**：AC 大规模重启后几百台 AP 同时重连，AC CPU 打满导致注册超时、AP 再重试，形成正反馈。AP 侧配置随机化重连退避，AC 侧限制每秒接入速率。

### 2. 供电与线路

| 标准 | PSE 输出 | PD 可用 |
|---|---|---|
| 802.3af | 15.4W | 12.95W |
| 802.3at | 30W | 25.5W |
| 802.3bt | 60/90W | 51/71W |

双频双流 AP 用 af 勉强够；Wi-Fi 6/6E 三频或 4x4 需要 at 甚至 bt。

常见事故：

- 交换机整机 PoE 预算不够（如 24 口交换机总预算仅 190W，插满 at 类 AP 必然掉电）
- 使用非标 PoE 供电器
- 线路超 100 米 —— 建议改光纤 + 光电转换或加中继
- **上电顺序**：AP 若在网络就绪前上电，可能直接发现超时

### 3. 漫游

用户抱怨最多的"走到走廊就卡"，本质是**终端粘滞（sticky client）**——终端只有信号极差时才切换。四个必做项：

1. **统一配置**：全网同 SSID、同加密方式、同密钥；隧道转发或跨 AP 同一业务 VLAN，保证漫游后 IP 不变。
2. **开启 11k/v/r**
   - `802.11k`：邻居报告，让终端知道去哪
   - `802.11v`：BSS Transition Management，AC 可主动引导终端切换
   - `802.11r`：快速 BSS 切换，压缩四次握手，对 VoWiFi 关键
   - 注意：部分老终端对 11r 兼容性差，建议 FT-over-DS + 混合模式，或对 IoT SSID 单独关闭
3. **RSSI 踢除门限**：如低于 -75dBm 且有更好邻居时主动断开。门限不要过于激进，否则边缘用户被反复踢。
4. **关闭低速率**：禁掉 1/2/5.5/11Mbps，basic rate 设 12 或 24Mbps。这一条能同时**缩小小区半径、减少管理帧空口开销、逼迫终端更早漫游**，性价比最高。

本地转发又必须跨 VLAN 漫游时，需要厂商的"漫游隧道 / Home Agent"机制，把漫游后的流量隧道回原 AP 或原 VLAN。

### 4. 射频与干扰

**2.4G**：只有 1/6/11 三个非重叠信道，高密场景基本无解。标准做法是**部分 AP 关闭 2.4G 射频**，只保留满足 IoT 覆盖的最小集合。

**5G**：用足 UNII-2/2e 的 DFS 信道，但要评估雷达误检导致的信道切换（机场、气象雷达附近慎用，可在 AC 上排除特定 DFS 信道）。

**带宽选择**（反直觉原则：密度越高，带宽越窄）

| 场景 | 推荐带宽 |
|---|---|
| 高密会场 | 20MHz（换信道数量） |
| 普通办公 | 40MHz |
| 家庭/低密 | 80MHz |

**发射功率**：不是越大越好。功率过大造成上下行不对称（AP 喊得远、终端回不来）和更大的同频干扰域。一般 5G 设 11~14dBm、2.4G 设 5~9dBm 起调。

**RRM**：开自动信道 + 自动功率，但要设扫描/调整时间窗（如夜间），避免业务高峰期切信道造成瞬断。

### 5. 高密容量

单 AP 并发预期：普通 2x2 舒适并发 25~30；高端 4x4 优化后 50~80。超过就要加 AP 或分频段。

- **SSID 数量控制在 3 个以内**。每个 SSID 每射频每 100ms 发一次 Beacon，8 个 SSID 双频就是每秒几百帧纯开销，且都用最低速率发送。
- **广播/组播抑制**：ARP 代理（AP 本地应答）、DHCP 广播转单播、组播转单播、IGMP Snooping、限制未知单播泛洪。大二层里 ARP 风暴是空口杀手。
- **Band Steering + 负载均衡**：双频终端引导到 5G，AP 间按用户数/信道利用率均衡。注意只支持 2.4G 的 IoT 设备会因此关联延迟，最好给 IoT 单独 SSID 并关闭该功能。
- **DTIM 调优**：省电优先设 3；实时业务（语音/视频）设 1。
- **组播业务**（IPTV、无线投屏）单独规划，必要时组播转单播 + 固定速率。

### 6. 认证与 Portal

**Portal 典型问题**：终端 CNA（Captive Network Assistant）弹窗探测失败；HTTPS 重定向无法完成（浏览器普遍 HSTS，只能靠 302 明文 HTTP 或 DHCP Option 114 / RFC 8910）；Portal 服务器与 AC 之间的心跳/免认证规则没放行 DNS。

**802.1X 典型问题**：RADIUS 超时（建议主备 RADIUS + 合理超时重传）、证书链不完整、MAC 旁路（MAB）兜底策略缺失。

**必配认证逃生**：RADIUS/Portal 全挂时，允许已在线用户保持、新用户按策略放行或进入受限 VLAN。否则一次认证服务器故障就是全场断网。

### 7. 可靠性与运维

- **AC 双机热备**：VRRP + 配置/表项同步，AP 双链路注册主备 AC。AC 单点是最大风险。
- **AP 逃生**：与 AC 断连后继续用最后一次配置本地转发服务，而不是直接停止射频。
- **NTP 统一**：否则跨设备日志无法关联。
- **syslog 集中收集**。
- **分批升级**：一次不超过 20%，避开业务时段。
- **配置模板化**：按楼层/场景做 AP Group，而不是逐台配。

### 8. 施工与勘测

方案再好，AP 装错位置也白搭。

- **房间部署优于走廊部署**：走廊部署要穿两堵墙，且走廊会变成同频干扰走廊
- AP 不要装在金属吊顶内、消防管道旁、承重柱背面
- 高密场馆考虑下挂式定向天线做扇区覆盖
- 正式交付前做 Site Survey：预测式仿真 + 落地实测

---

## 三、容易被忽略的细节

### MTU / 分片

CAPWAP 隧道封装约 24~30 字节 + UDP/IP 头，内层 1500 字节报文出隧道会分片。

**典型表象**：能上网，但打不开某些网站、大文件传输卡死。

**解决**：中间链路 MTU 提到 1600+（jumbo frame），或在 AC/网关做 TCP MSS clamping（一般钳到 1360~1400）。

### 转发方式混合使用

不必全网统一。语音、需跨楼漫游的业务走隧道转发保 IP；普通办公/访客上网走本地转发减轻 AC 压力。大部分厂商支持按 SSID 或按 VLAN 指定转发方式。

### IP 规划

- 单个业务大二层广播域不超过 `/22`（约 1000 个地址），再大就用多 VLAN + 漫游隧道
- DHCP 租期：高流动场景（展馆、机场）30~60 分钟；固定办公 8~24 小时。避免地址池假性耗尽

---

## 四、直接转发 vs 隧道转发

### 4.1 原理区别

两者**控制面完全一样**：AP 都通过 CAPWAP 控制隧道（UDP 5246）向 AC 注册、拿配置、上报状态。区别只在**数据面**——用户业务报文走不走 AC。

**直接转发（本地转发 / Local Bridging）**

```
终端 --802.11--> AP: 解密、802.11转802.3、打业务VLAN tag
                  |
                  v
            接入交换机(Trunk口) --> 汇聚 --> 核心/网关 --> 出口

AC 只在旁边收发 CAPWAP 控制报文，看不到一个用户数据包
```

AP 相当于"无线网卡 + 二层交换机"，把 802.11 帧还原成普通以太帧，按 SSID 映射的业务 VLAN 打 tag，从有线口丢出去。之后就是纯粹的有线转发，与 AC 无关。

**隧道转发（集中转发 / CAPWAP Tunnel）**

```
终端 --802.11--> AP: 解密、802.11转802.3
                  |  封装: [外层IP(AP管理IP -> AC IP)][UDP 5247][CAPWAP头][原始以太帧]
                  v
            接入交换机(Access口, 只有管理VLAN) --> 汇聚 --> AC
                                                        |
                                                  AC: 解封装, 剥掉外层
                                                  打业务VLAN tag, 从上行口送出
                                                        v
                                                    核心/网关 --> 出口
```

对中间交换机而言，这只是一堆 AP 管理网段之间的 UDP 单播报文，它们完全不知道里面装的是无线用户流量。**业务 VLAN 只存在于 AC 的上行链路上。**

> **补充**：加解密（WPA2/WPA3 的 CCMP）现在两种模式下基本都在 AP 完成（Local MAC 架构）。早期有方案把 802.11 原始帧整个隧道到 AC 解密（Split MAC），现已少见，因为 AC 加解密性能撑不住。

### 4.2 衍生的实际差异

**接入交换机配置量** —— 最直接的差异

- 直接转发：每个 AP 端口配 Trunk（管理 VLAN 做 PVID/untagged + 所有业务 VLAN tagged），且从 AP 到网关每一段链路都要放通。几百台 AP 就是几百个端口，容易漏配某段汇聚链路导致"某几台 AP 下的用户上不了网"。
- 隧道转发：AP 口就是 Access 口，扔进管理 VLAN 即可。**改造老网络时优势极大**，尤其当有线网络不归你管或交换机太老时。

**AC 性能压力**

隧道转发下 AC 转发吞吐是硬天花板。粗算：标称 20Gbps 的 AC 带 300 台 Wi-Fi 6 AP，平均每 AP 仅 66Mbps。宿舍、写字楼这类人均带宽需求高的场景，AC 会先于空口成为瓶颈。直接转发完全没有这个问题，低配 AC 带上千 AP 都行。

**时延与发夹弯（hairpin）**

隧道转发下，同一台 AP 下两终端互访（无线投屏、AirDrop、打印机、局域网游戏），报文要隧道到 AC、解封装、再隧道回同一台 AP，绕一个大圈。局域网互访体验明显更差。

**漫游**（常见误解）

"直接转发不能无缝漫游"是错的。只要漫游范围内 AP 都能接入同一业务 VLAN（大二层打通），直接转发同样是 IP 不变的无缝漫游。真正区别在于：

- 隧道转发的大二层是 AC **虚拟**出来的，物理网络怎么切三层都无所谓
- 直接转发的大二层必须靠有线网络**实打实**把 VLAN 拉通

所以跨楼、跨园区、跨三层场景隧道转发省事得多，直接转发需依赖厂商漫游隧道来补。

**故障域**

- 隧道转发：AC 宕机 = 全网无线业务中断（除非 AP 逃生切本地转发，但已有会话仍断）
- 直接转发：AC 宕机只失去集中管理，用户业务毫发无损

**策略执行点**

ACL、限速、行为审计、流量统计：隧道转发可在 AC 集中做，一点管全网；直接转发要下沉到 AP（表项和 CPU 有限）或接入交换机（配置分散）。有等保、审计留存、访客流量必须从指定出口走等合规要求时，隧道转发几乎是唯一省心的选择。

**MTU**

隧道转发多约 30~40 字节封装，必须开巨帧或做 MSS clamp，否则会出现极难排查的故障（见[第三章](#mtu--分片)）。直接转发无此问题。

**组播**

隧道转发下组播流到 AC 后要为每个有接收者的 AP 复制一份单播隧道。一路 IPTV 组播带 100 个 AP 就是 100 份复制流，AC 和上行链路容易被打爆（除非厂商支持组播隧道优化）。直接转发配合接入层 IGMP Snooping 就是正常组播复制，压力小得多。

### 4.3 差异速查

| 维度 | 直接转发 | 隧道转发 |
|---|---|---|
| 数据面经过 AC | 否 | 是 |
| 接入交换机配置 | Trunk，全路径放通业务 VLAN | Access，仅管理 VLAN |
| AC 吞吐瓶颈 | 无 | 有，需容量规划 |
| AP 间互访时延 | 低 | 高（发夹弯） |
| 跨三层漫游 | 需漫游隧道补充 | 天然支持 |
| AC 故障影响 | 仅失管理 | 业务中断 |
| 集中策略/审计 | 需下沉，分散 | 天然集中 |
| MTU 处理 | 无需处理 | 必须巨帧或 MSS clamp |
| 组播效率 | 高 | 低（AC 复制） |
| 无线/有线隔离 | 需交换机侧做 | 天然隔离 |

---

## 五、转发模式选型

### 三个决策问题

1. **有线网络归不归你管、能不能改？**
   不能改（老旧交换机、第三方物业网络、跨运营商专线到分支）→ 隧道转发。能自由配 VLAN → 直接转发可选。

2. **流量规模多大？**

   ```
   AC 需要的吞吐 ≈ AP 数 × 每 AP 平均实际吞吐 × 走隧道的流量占比
   ```

   再留 50% 余量。算出来超过 AC 标称转发能力的一半，就别全隧道。

3. **有没有集中审计/合规/统一出口的硬要求？**
   有 → 至少这部分业务走隧道。

### 按场景推荐

| 场景 | 推荐 |
|---|---|
| 校园宿舍、写字楼办公（大流量、AP 多） | 直接转发 |
| 酒店、医院（AP 多、单用户流量小、需集中策略） | 隧道转发或混合 |
| 商超连锁、分支机构 AP 回总部 AC | 控制面隧道 + 数据本地转发 |
| 展馆、机场（跨大范围漫游、跨三层） | 隧道转发 |
| 访客 / BYOD / IoT | 隧道转发到 DMZ |
| 有线网络不可改造的存量项目 | 隧道转发 |

### 推荐方案：混合模式

绝大多数厂商支持按 SSID（部分还能按 VLAN 或用户组）指定转发方式：

- **员工 SSID → 直接转发**：大流量留在接入层，性能最好
- **访客 SSID → 隧道转发**：直送 DMZ 或独立出口，天然与内网隔离，审计和限速在 AC 一把抓
- **语音 / 跨楼漫游 SSID → 隧道转发**：保证漫游 IP 不变

既避开 AC 吞吐瓶颈，又拿到集中管控和安全隔离的好处。

---

## 六、在 OpenWrt 上的落地

| 能力 | OpenWrt 对应实现 |
|---|---|
| 802.11r/k/v 漫游 | `hostapd`：`ieee80211r`、`ft_over_ds`、`rrm_neighbor_report`、`bss_transition`；UCI 对应 wifi-iface 的 `ieee80211r`/`ieee80211k`/`ieee80211v` |
| 最低速率 | `basic_rate` / `supported_rates` |
| RSSI 踢除、Band Steering、11v 引导 | **`usteer`**（OpenWrt 官方的 AP 引导/负载均衡守护进程，最接近"轻量 AC 射频管理"） |
| 射频参数下发（信道、带宽、功率、国家码） | netifd 的 `wireless-device.uc`（`package/network/config/wifi-scripts/`） |
| 集中管理 / AP 注册 | 需外部控制器：OpenWISP、TR-069（`easycwmp`） |
| Portal 认证 | `uspot`、`opennds` |
| 直接转发 | wifi-iface 桥接到带 VLAN 的 `br-lan`（`bridge-vlan` 配置），AP 原生就是这个模型 |
| 隧道转发 | OpenWrt **无原生 CAPWAP 数据隧道**，用 VXLAN / GRE / L2TPv3 到中心网关，再把 wifi-iface 桥进隧道口。效果等价，同样要处理 MTU 和 MSS clamp |

集中射频调优时，控制器最终就是通过 `wireless-device.uc` 对应的 UCI/ubus 接口生效的。

> 各层组件的详细职责、调用链和代码位置，见 [`ac-ap-software-architecture.md`](./ac-ap-software-architecture.md)。

---

## 附录：交付验收指标

| 指标 | 目标值 |
|---|---|
| 目标区域 5G RSSI | ≥ -65dBm |
| SNR | ≥ 25dB |
| 同频干扰 AP 数 | ≤ 3 |
| 漫游切换时延（启用 11r） | < 100ms |
| 单 AP 并发（2x2 / 4x4） | 25~30 / 50~80 |
